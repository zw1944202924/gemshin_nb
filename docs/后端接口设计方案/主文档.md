# 后端接口设计方案

## 1. 接口设计原则

- **RESTful API设计**：遵循RESTful API设计规范，使用HTTP方法（GET, POST, PUT, DELETE）表示操作类型
- **模块化设计**：按照系统模块划分API路由，使用Flask蓝图(Blueprint)组织API路由
- **参数验证**：使用Flask-WTF等库验证请求参数的合法性
- **响应格式统一**：所有API返回JSON格式数据，包含状态码、消息和数据
- **错误处理**：统一处理错误，返回标准错误响应
- **认证授权**：使用JWT实现用户认证和权限控制

## 2. 通用响应格式

```json
{
  "code": 200,              // 状态码
  "message": "成功",        // 消息
  "data": {}                // 数据
}
```

## 3. 模块划分

- **用户管理模块**：处理用户认证和权限
- **股票分析模块**：提供股票基本面和技术面分析
- **持仓管理模块**：管理用户的股票持仓
- **关注列表监控模块**：实时监控用户关注的股票，设置触发条件并发送通知
- **分析报告管理模块**：管理历史分析报告，支持导出和分享
- **交易记录管理模块**：管理用户交易记录，支持统计分析
- **市场板块分析模块**：分析市场板块表现和相关性
- **数据导入/导出模块**：支持数据的导入和导出
- **通知中心模块**：管理系统通知和提醒
- **用户反馈模块**：收集和处理用户反馈
- **AI模型管理模块**：管理AI模型的配置和性能

## 4. 认证与授权

### 4.1 JWT认证流程

1. 用户通过 `/api/v1/auth/login` 接口获取访问令牌
2. 前端在后续请求的 `Authorization` 头中携带令牌：`Bearer {access_token}`
3. 后端验证令牌的有效性
4. 对于需要权限的接口，验证用户是否具有相应权限

### 4.2 权限控制

- **无权限**：无需登录即可访问的接口，如登录、注册等
- **已登录**：需要用户登录才能访问的接口，如获取用户信息、查看持仓等
- **管理员**：需要管理员权限才能访问的接口，如AI模型管理等

### 4.3 管理员权限实现

1. **管理员账户创建**：
   - 系统初始化时创建默认管理员账户
   - 管理员可以通过管理界面创建新的管理员账户

2. **权限验证实现**：
   - 使用Flask的装饰器实现权限验证
   - 在用户模型中添加 `is_admin` 字段标识管理员身份
   - 对于需要管理员权限的接口，使用 `@admin_required` 装饰器

3. **权限管理接口**：
   - `/api/v1/admin/user/list`：获取用户列表
   - `/api/v1/admin/user/{id}/role`：修改用户角色
   - `/api/v1/admin/user/{id}/status`：启用/禁用用户

## 5. 错误处理

| 状态码 | 描述 | 示例响应 |
|-------|------|----------|
| 400 | 请求参数错误 | `{"code": 400, "message": "请求参数错误", "data": {"field": "email", "error": "邮箱格式不正确"}}` |
| 401 | 未授权 | `{"code": 401, "message": "未授权，请登录", "data": {}}` |
| 403 | 禁止访问 | `{"code": 403, "message": "禁止访问", "data": {}}` |
| 404 | 资源不存在 | `{"code": 404, "message": "资源不存在", "data": {}}` |
| 500 | 服务器内部错误 | `{"code": 500, "message": "服务器内部错误", "data": {}}` |

## 6. 接口安全

- **HTTPS**：使用HTTPS协议加密传输数据
- **JWT令牌**：使用JWT进行身份验证，设置合理的过期时间
- **参数验证**：对所有请求参数进行验证，防止注入攻击
- **CORS**：设置合理的CORS策略，防止跨站请求伪造
- **速率限制**：对API请求进行速率限制，防止暴力攻击
- **敏感数据加密**：对敏感数据进行加密存储

## 7. 性能优化

- **缓存**：使用Redis缓存热点数据，减少数据库查询
- **异步处理**：使用Celery处理耗时的任务，如数据采集和模型分析
- **分页**：对列表接口实现分页，减少数据传输量
- **批量操作**：支持批量操作，减少API调用次数
- **索引优化**：对数据库查询进行索引优化

## 8. 部署与集成

- **本地开发**：使用Flask内置服务器进行开发
- **生产部署**：使用Gunicorn作为WSGI服务器，Nginx作为反向代理
- **环境变量**：使用环境变量配置敏感信息，如数据库连接、API密钥等
- **日志**：配置详细的日志记录，便于排查问题

## 9. 版本控制

- **API版本**：在URL中包含API版本，如 `/api/v1/stock`
- **向后兼容**：确保API版本升级时的向后兼容性
- **版本管理**：使用语义化版本号管理API版本

## 10. 接口测试

- **单元测试**：对每个接口进行单元测试
- **集成测试**：测试接口之间的协作
- **性能测试**：测试接口的响应速度和稳定性
- **安全性测试**：测试接口的安全性

## 11. 文档维护

### 11.1 API文档生成

- **使用Swagger**：集成Flask-RESTPlus或Flask-RESTX库生成Swagger文档
- **配置方法**：
  1. 安装依赖：`pip install flask-restx`
  2. 在应用中初始化Swagger：
     ```python
     from flask_restx import Api
     
     api = Api(
         app,
         version='1.0',
         title='AI股票分析助手API',
         description='AI股票分析助手后端API文档',
         doc='/api/docs'
     )
     ```
  3. 为每个路由添加文档注释：
     ```python
     from flask_restx import Resource, fields
     
     auth_ns = api.namespace('auth', description='认证相关接口')
     
     register_model = auth_ns.model('Register', {
         'username': fields.String(required=True, description='用户名', min_length=3, max_length=20),
         'email': fields.String(required=True, description='邮箱', example='user@example.com'),
         'password': fields.String(required=True, description='密码', min_length=6, max_length=20)
     })
     
     @auth_ns.route('/register')
     class Register(Resource):
         @auth_ns.expect(register_model)
         @auth_ns.response(200, '注册成功')
         @auth_ns.response(400, '请求参数错误')
         def post(self):
             """用户注册"""
             # 实现注册逻辑
             pass
     ```

### 11.2 文档访问

- **访问地址**：`http://localhost:5000/api/docs`
- **文档格式**：支持Swagger UI和ReDoc两种格式
- **导出功能**：支持导出API文档为JSON或YAML格式

### 11.3 文档更新

- **自动更新**：当代码中的API定义发生变化时，文档会自动更新
- **手动更新**：可以通过重新启动应用来更新文档
- **版本控制**：在文档中包含API版本信息，确保文档与代码版本一致

### 11.4 示例代码

- **前端调用示例**：
  ```javascript
  // 使用Axios调用登录接口
  import axios from 'axios';
  
  async function login(email, password) {
    try {
      const response = await axios.post('/api/v1/auth/login', {
        email: email,
        password: password
      });
      return response.data;
    } catch (error) {
      console.error('登录失败:', error);
      throw error;
    }
  }
  
  // 使用JWT令牌调用需要认证的接口
  async function getPortfolio() {
    try {
      const token = localStorage.getItem('access_token');
      const response = await axios.get('/api/v1/portfolio', {
        headers: {
          'Authorization': `Bearer ${token}`
        }
      });
      return response.data;
    } catch (error) {
      console.error('获取持仓失败:', error);
      throw error;
    }
  }
  ```

- **后端调用示例**：
  ```python
  import requests
  
  # 登录获取令牌
  def login(email, password):
      response = requests.post('http://localhost:5000/api/v1/auth/login', {
          'email': email,
          'password': password
      })
      return response.json()
  
  # 使用令牌调用接口
  def get_stock_analysis(stock_code, analysis_type, token):
      headers = {
          'Authorization': f'Bearer {token}'
      }
      response = requests.post(
          f'http://localhost:5000/api/v1/stock/{stock_code}/analysis',
          json={'analysis_type': analysis_type},
          headers=headers
      )
      return response.json()
  ```

## 12. 未来扩展

- **微服务拆分**：将大型模块拆分为独立的微服务
- **WebSocket**：使用WebSocket实现实时数据推送
- **GraphQL**：考虑使用GraphQL提供更灵活的API查询
- **第三方集成**：提供API接口，支持第三方应用集成