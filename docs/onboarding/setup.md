# Local Setup

1. Copy `.env.example` to `.env`
2. Start infrastructure with `docker compose up -d`
3. Install Python dependencies in `apps/api`
4. Install Node dependencies in `apps/web`
5. Run API and web locally
