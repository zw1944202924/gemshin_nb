# Product

## Register

product

## Users

主要用户是围绕 `gemshin_nb` 持续扩展业务模块的人和协同 agent，同时也包括未来会进入具体业务系统执行内容生产、项目管理和状态跟进的业务使用者。当前高频场景以桌面 Web 为主，用户通常先从平台级入口选择模块，再进入具体业务系统和项目工作台。

## Product Purpose

`gemshin_nb` 是一个多业务模块共存的产品底座。它需要用统一的平台壳层、账号、权限和模块中心承接不同业务系统，其中 `AI动画制作系统` 不是独立站点，而是平台内的一条完整业务线，负责从项目建立、章节/分镜/镜头推进到素材、任务和导出的生产链路组织。

成功标准是：平台级导航与业务级导航分层清晰；业务系统、项目和镜头层级不混淆；静态 Demo、设计方案和后续实现能围绕同一套信息架构推进，而不是每轮重新解释页面到底属于哪个层级。

## Brand Personality

克制、清晰、可靠。

整体表达以任务推进和结构辨认为先，不依赖夸张包装和花哨动效。用户应该能快速看懂自己现在位于平台、系统、项目还是镜头层，并自然判断下一步该去哪里继续工作。

## Anti-references

- 不做脱离 `gemshin_nb` 模块中心的独立业务站点。
- 不把系统页、项目页、镜头页和全局工具页扁平并列成同一层导航。
- 不照搬桌面客户端的窗口控制条、多标签工作区和重工具箱侧栏。
- 不用面向评审者的解释文案代替真实用户可用的导航与状态结构。

## Design Principles

- 平台层、系统层、项目层、镜头层必须逐级收口，不做跨层捷径。
- 全局素材与全局任务、项目素材与项目任务分开归属，避免同名不同义。
- 页面靠导航、上下文和状态自证结构，而不是靠大段说明文案解释。
- 共享设计令牌和组件语言优先于单页个性，保证系统扩展时仍然一致。
- 每个 Demo 必须可复核、可截图、可交接，能直接支撑下一步评审。

## Accessibility & Inclusion

默认按 WCAG AA 目标处理正文对比度、按钮可读性和窄屏结构收缩。移动端优先保留主导航、当前上下文和关键动作，不让用户依赖 hover 才能理解页面。后续真实实现阶段继续补齐键盘焦点、reduced motion、空状态与错误状态的可达性。
